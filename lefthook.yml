pre-commit:
  commands:
    prettier:
      glob: "*.{ts,tsx,mts,mjs,json,css,md}"
      run: pnpm prettier --write {staged_files}
      stage_fixed: true
    eslint:
      glob: "*.{ts,tsx,mts,mjs}"
      run: pnpm eslint --fix {staged_files}
      stage_fixed: true
